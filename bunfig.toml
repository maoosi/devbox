[install]
exact = true
minimumReleaseAge = 604800 # 7 days in seconds
# @upstash/box releases faster than the age gate; trusted first-party dep of packages/upstash.
minimumReleaseAgeExcludes = ["@upstash/box"]
